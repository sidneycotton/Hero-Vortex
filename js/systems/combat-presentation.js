/* Hero Vortex — deliberate combat choreography. */
(() => {
  const WAIT_BETWEEN_ACTIONS = 1000;
  const ACTION_CUE_MS = 520;
  const STEP_MS = 1200;
  let pacingInstalled = false;

  const iconName={damage:'sword',heal:'heart',shield:'shield',status:'target',buff:'spark',summon:'spark',field:'spark',taunt:'target',sacrifice:'sword',revive:'heart',move:'target',delayed:'spark'};
  const kindOf=e=>{if(!e)return'buff';if(['dealDamage','conditionalDamage','conditionalRepeat'].includes(e.type))return'damage';if(['heal','conditionalHeal','spendCounterToHeal','conditionalLifesteal'].includes(e.type))return'heal';if(e.type==='applyShield')return'shield';if(e.type==='applyStatus')return'status';if(['gainCounter','buffMaxLife','conditionalBuff'].includes(e.type))return'buff';if(e.type==='createToken')return'summon';if(e.type==='sacrificeToken')return'sacrifice';if(e.type==='applyFieldEffect')return'field';if(e.type==='taunt')return'taunt';if(e.type==='reviveCopy')return'revive';if(e.type==='moveNow')return'move';if(e.type==='delayedEffect')return'delayed';return'buff'};
  const sound=k=>window.HVAudio?.play({damage:'impact',heal:'heal',shield:'shield',status:'status',buff:'buff',summon:'summon',field:'field',taunt:'buff',sacrifice:'sacrifice',revive:'revive',move:'tap',delayed:'status'}[k]||'buff');
  const el=uid=>document.querySelector(`.unit-card[data-uid="${uid}"]`);
  const point=uid=>{const e=el(uid);if(!e)return null;const r=e.getBoundingClientRect();return{x:r.left+r.width/2,y:r.top+r.height/2,w:r.width,h:r.height}};
  const layer=()=>document.getElementById('hvFxLayer')||document.body;
  const svg=k=>typeof window.svgIcon==='function'?window.svgIcon(iconName[k]||'spark'):'';

  function actionCue(uid,k){const e=el(uid);if(!e)return;e.querySelector('.hv-action-cue')?.remove();const cue=document.createElement('div');cue.className=`hv-action-cue hv-action-${k}`;cue.innerHTML=`<span>${svg(k)}</span><b>${({damage:'ATAQUE',heal:'CURA',shield:'ESCUDO',status:'STATUS',buff:'BUFF',summon:'INVOCAR',sacrifice:'SACRIFÍCIO',field:'CAMPO',taunt:'PROVOCAÇÃO',revive:'REVIVER',move:'MOVER',delayed:'EFEITO'})[k]||'AÇÃO'}</b>`;e.appendChild(cue);setTimeout(()=>cue.remove(),ACTION_CUE_MS)}
  function cueNextAction(){const item=window.state?.resolutionQueue?.[window.state?.resolutionIdx];if(!item)return;const caster=getUnit(item.uid),ability=CARD_DB[caster?.cardId]?.abilities?.[item.abilityIdx];if(caster&&ability)actionCue(item.uid,kindOf(ability.effects?.[0]))}
  function glyph(x,y,k,cls=''){const g=document.createElement('div');g.className=`hv-combat-glyph hv-combat-${k} ${cls}`;g.style.left=`${x}px`;g.style.top=`${y}px`;g.innerHTML=svg(k);layer().appendChild(g);setTimeout(()=>g.remove(),1500);return g}
  function targetIds(effect,diff){if(diff.targetUid)return[diff.targetUid];const groups=['allEnemies','allEnemiesFieldAndHand','allAllies','allAlliesIncludingHand','enemyDefensor','enemyAtacante','enemySuporte','allyAtacante','allyDefensor'];if(groups.includes(effect?.target))return Object.keys(diff.after||{}).filter(uid=>diff.after[uid]&&!diff.after[uid].dead);if(effect?.target==='self')return[diff.casterUid];if(effect?.target==='lastTarget')return diff.targetUid?[diff.targetUid]:[];return[]}

  function singleStrike(casterUid,targetUid){const s=point(casterUid),t=point(targetUid);if(!s||!t)return;const ghost=el(casterUid)?.cloneNode(true);if(!ghost)return;ghost.className='hv-combat-card-ghost hv-strike-ghost';Object.assign(ghost.style,{left:`${s.x-s.w/2}px`,top:`${s.y-s.h/2}px`,width:`${s.w}px`,height:`${s.h}px`});layer().appendChild(ghost);sound('damage');ghost.animate([{transform:'translate3d(0,0,0) scale(1)',opacity:.95},{transform:`translate3d(${(t.x-s.x)*.6}px,${(t.y-s.y)*.6}px,0) scale(1.04) rotate(${t.x>=s.x?3:-3}deg)`,opacity:1,offset:.42},{transform:`translate3d(${t.x-s.x}px,${t.y-s.y}px,0) scale(.97) rotate(${t.x>=s.x?7:-7}deg)`,opacity:1,offset:.62},{transform:`translate3d(${(t.x-s.x)*.35}px,${(t.y-s.y)*.35}px,0)`,opacity:.9,offset:.76},{transform:'translate3d(0,0,0) scale(1)',opacity:0}],{duration:1150,easing:'cubic-bezier(.2,.76,.16,1)',fill:'forwards'}).finished.then(()=>ghost.remove()).catch(()=>ghost.remove());const sword=glyph(s.x,s.y,'damage','hv-sword-projectile');sword.animate([{transform:'translate(-50%,-50%) scale(.2) rotate(-35deg)',opacity:0},{transform:`translate(${t.x-s.x}px,${t.y-s.y}px) scale(1.2) rotate(20deg)`,opacity:1,offset:.78},{transform:`translate(${t.x-s.x}px,${t.y-s.y}px) scale(.6) rotate(45deg)`,opacity:0}],{duration:900,easing:'cubic-bezier(.18,.8,.15,1)',fill:'forwards'});setTimeout(()=>{el(targetUid)?.classList.add('hv-impact-shake');glyph(t.x,t.y,'damage','hv-impact-flare')},760)}
  function areaDamage(casterUid,targets){const s=point(casterUid);if(!s)return;sound('damage');glyph(s.x,s.y,'damage','hv-area-core');targets.forEach((uid,i)=>{const t=point(uid);if(!t)return;setTimeout(()=>{glyph(t.x,t.y,'damage','hv-impact-flare');el(uid)?.classList.add('hv-impact-shake')},300+i*150)})}
  function travel(k,casterUid,targetUid){const s=point(casterUid),t=point(targetUid);if(!s||!t)return;const g=glyph(s.x,s.y,k,'hv-travel');g.animate([{transform:'translate(-50%,-50%) scale(.25)',opacity:0},{transform:`translate(${t.x-s.x}px,${t.y-s.y}px) scale(1.15)`,opacity:1,offset:.78},{transform:`translate(${t.x-s.x}px,${t.y-s.y-20}px) scale(.7)`,opacity:0}],{duration:900,easing:'cubic-bezier(.18,.8,.15,1)',fill:'forwards'}).finished.then(()=>g.remove()).catch(()=>g.remove())}
  function animateEffect(effect,diff,delay){const k=kindOf(effect),targets=targetIds(effect,diff);setTimeout(()=>{sound(k);if(k==='damage'){const area=['allEnemies','allEnemiesFieldAndHand','enemyDefensor','enemyAtacante','enemySuporte'].includes(effect?.target)||targets.length>1;area?areaDamage(diff.casterUid,targets):targets[0]&&singleStrike(diff.casterUid,targets[0])}else if(k==='heal'||k==='buff')targets.forEach((uid,i)=>setTimeout(()=>{travel(k,diff.casterUid,uid);const p=point(uid);if(p)glyph(p.x,p.y,k,'hv-target-burst')},i*160));else if(k==='shield')targets.forEach((uid,i)=>setTimeout(()=>{const t=point(uid);if(t){el(uid)?.classList.add('hv-shield-pulse');glyph(t.x,t.y,k,'hv-shield-dome')}},i*140));else if(k==='status')targets.forEach((uid,i)=>setTimeout(()=>{const t=point(uid);if(t){el(uid)?.classList.add('hv-status-pulse');glyph(t.x,t.y,k,'hv-status-rune')}},i*150));else if(k==='summon'){const ids=Object.keys(diff.after||{}).filter(uid=>!(diff.before||{})[uid]);ids.forEach((uid,i)=>setTimeout(()=>{const t=point(uid);if(t){glyph(t.x,t.y,k,'hv-summon-burst');el(uid)?.classList.add('hv-summon-enter')}},250+i*180))}else if(k==='sacrifice'){const source=point(diff.casterUid),ids=Object.keys(diff.before||{}).filter(uid=>!(diff.after||{})[uid]);ids.forEach((uid,i)=>{const t=point(uid);if(!source||!t)return;setTimeout(()=>{glyph(t.x,t.y,k,'hv-sacrifice-burst');glyph(source.x,source.y,k,'hv-sacrifice-core')},i*180)})}else if(k==='revive')targets.forEach((uid,i)=>setTimeout(()=>{const t=point(uid);if(t){glyph(t.x,t.y,k,'hv-revive-rune');el(uid)?.classList.add('hv-revive-enter')}},i*160));else if(k==='field'){const s=document.querySelector('.hv-battle-stage')?.getBoundingClientRect();if(s){glyph(s.left+s.width/2,s.top+s.height/2,k,'hv-field-weather');document.querySelector('.hv-battle-stage')?.classList.add('hv-field-wave')}}else if(k==='taunt')targets.forEach(uid=>{const t=point(uid);if(t){el(uid)?.classList.add('hv-taunt-shake');glyph(t.x,t.y,k,'hv-taunt-mark')}});else if(k==='move')targets.forEach(uid=>{const t=point(uid);if(t)glyph(t.x,t.y,k,'hv-move-arrow')});else if(k==='delayed'){const s=point(diff.casterUid);if(s)glyph(s.x,s.y,k,'hv-delayed-rune')}},delay)}
  function playCombatSequence(diff){if(!diff)return;const caster=getUnit(diff.casterUid),ability=CARD_DB[caster?.cardId]?.abilities?.[diff.abilityIdx];if(!caster||!ability)return;let delay=0;for(const effect of ability.effects||[]){animateEffect(effect,diff,delay);delay+=STEP_MS}}
  function installPacing(){if(pacingInstalled||typeof window.autoResolveStep!=='function')return;pacingInstalled=true;const original=window.autoResolveStep;window.autoResolveStep=function pacedResolve(){cueNextAction();window.setTimeout(()=>{if(window.state?.phase==='resolve')original()},WAIT_BETWEEN_ACTIONS)}}
  window.playCombatSequence=playCombatSequence;installPacing();window.addEventListener('load',installPacing,{once:true});
})();